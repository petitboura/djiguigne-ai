# Copier en .env.local (jamais commité) et remplir avec les vraies valeurs.

# Mêmes valeurs que côté app (djiguigne-frontend/.env.local.example) : même
# projet Supabase, mêmes comptes utilisateurs. Supabase → Settings → API.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Requis par les pages créateur copiées depuis l'app (app/dashboard/...) :
# lib/api.ts en a besoin pour compiler/tourner. Ces pages ne sont pas
# encore reliées à la navigation du vitrine — voir discussion du 27/07.
NEXT_PUBLIC_API_URL=
